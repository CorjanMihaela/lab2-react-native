import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    marginTop: 60
  },
  title: {
    fontSize: 22,
    fontWeight: 'bold',
    marginBottom: 20
  },
  image: {
    width: 200,
    height: 120,
    resizeMode: 'contain'
  },
  name: {
    marginTop: 15,
    fontSize: 18,
    fontWeight: '600'
  }
});
